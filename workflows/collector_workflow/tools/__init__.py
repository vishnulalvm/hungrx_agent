# LangGraph tool bindings for the collector workflow's nodes (e.g. crawler
# fetch/screenshot tools, source-authority lookup, extraction model
# invocation) land here once the corresponding node logic is implemented.
# Empty for now — no node in this graph calls a tool yet.
