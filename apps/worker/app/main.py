import time

if __name__ == "__main__":
    # Placeholder run loop so the container stays up under `restart:
    # unless-stopped` until real job-processing logic is implemented.
    while True:
        time.sleep(60)
